# 2026-06-08 开发日志

## 今日完成

- 明确第一版产品需求、内容规则和默认设置。
- 确认采用 SwiftUI、SwiftData、系统语音和系统通知。
- 创建 Swift Package、应用入口、核心数据模型和基础服务。
- 创建项目文档体系、Codex 工作指引和每日开发日志自动任务。
- 完成今日学习、已学会、生词本和设置四个主页面。
- 完成 300 个词汇条目、60 条语法及相关短语和用法说明。
- 完成系统发音、学习状态切换、历史事件、搜索和数据清理。
- 完成每天上午 9:30 的默认通知与自定义提醒设置。
- 创建正式 Xcode macOS 应用工程和运行说明。
- 对 Release 应用进行本机签名，并安装到 `/Applications/英语学习助手.app` 供启动台使用。
- 将应用改为 macOS 顶部菜单栏形态，点击书本图标展开学习面板。
- 隐藏 Dock 图标，并在侧边栏增加明确的退出应用入口。
- 优化学习状态反馈：“学会了”按钮点击成功后显示为“已学会”。
- 将“已学会”类目的操作改为“取消已学会”，取消后恢复为未标记状态。
- 点击“学会了”后，将卡片从今日学习移到“已学会”类目；取消后可恢复到当天列表。
- 点击“加入生词本”后，将卡片从今日学习移到“生词本”类目。
- 在侧边栏“已学会”和“生词本”标题右侧增加实时总数徽标。
- 将 300 条词汇内容源替换为日常生活场景数据，覆盖居家、饮食、购物、交通、社交和日常办事。
- 保留原有稳定内容 ID，避免已有“已学会”和“生词本”记录失联。
- 校正 60 条语法中的例句，使其更贴近日常生活表达。
- 将应用主题强调色统一调整为橘色，覆盖侧边栏选中态、标题、进度条、主要操作、语法标签、卡片边框和例句强调线。

## 验证结果

- 已确认本机使用 Xcode 16.2、Swift 6.0.3。
- `swift build` 构建成功。
- `swift test` 最终 5 项测试全部通过，覆盖内容数量、记忆辅助、默认设置、每日稳定生成和状态历史。
- Xcode Debug 与 Release `.app` 均构建成功，Debug 版本已执行启动命令。
- 已刷新 macOS LaunchServices 注册并成功发起安装版应用启动。
- 命令行裸可执行文件不支持系统通知，已改为正式 `.app` 工程并添加保护。
- 菜单栏改造后 `swift test` 的 5 项测试全部通过。
- 菜单栏改造后的 Xcode Debug `.app` 构建成功，并确认成品 `Info.plist` 中 `LSUIElement = 1`。
- 菜单栏改造后的 Xcode Release `.app` 构建成功，通过本机临时签名校验。
- 已覆盖安装 `/Applications/英语学习助手.app` 并成功发起新版启动；安装包确认 `LSUIElement = 1`。
- 日常生活内容源替换后 `swift test` 的 7 项测试全部通过，包含规模、独立场景例句和旧模板回归检查。
- 包含新内容源的 Xcode Release `.app` 构建成功。
- 橘色主题修改后 `swift test` 的 7 项测试全部通过。
- 强化橘色主题后 `swift test` 的 7 项测试全部通过，Xcode Release `.app` 构建成功。
- 已覆盖安装 `/Applications/英语学习助手.app` 并重新启动新版应用。

## 待办事项

- 继续人工校对自动扩展的短语和中文表达。
- 增加应用图标和可分发的签名构建。
- 在实际通知时间到达时完成一次端到端通知验收。
- 人工确认菜单栏面板在小尺寸屏幕和多显示器环境下的位置与可用性。

## 风险或阻塞

- 自动生成的学习内容需要抽样校对自然度和准确性。
- 当前构建未进行开发者签名，仅适合本机开发和运行。
- 当前环境无法读取系统进程列表，菜单栏图标显示与面板展开仍需人工目视确认。
